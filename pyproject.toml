[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "redtide-release-infra"
version = "1.0.0"
requires-python = ">=3.11"
description = "Release Infrastructure v1 control plane"

[project.scripts]
releasectl = "release_infra.cli:main"
